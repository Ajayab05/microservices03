apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: {{ .Values.namespace }}

spec:
  type: ClusterIP

  selector:
    app: user-service

  ports:
  - port: 80
    targetPort: 3001
